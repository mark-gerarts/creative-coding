{
  "name": "pushing-grid",
  "version": "0.1.0",
  "description": "Breakout implemented using PureSCript",
  "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "build-watch": "pulp --watch browserify --to dist/main.js",
      "start": "concurrently \"browser-sync start --server -w\" \"npm run build-watch\""
  },
  "homepage": "https://github.com/mark-gerarts/creative-coding",
  "dependencies": {
      "p5": "^0.9.0"
  },
  "devDependencies": {
      "browser-sync": "^2.26.7",
      "concurrently": "^4.1.1"
  }
}
